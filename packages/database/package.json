{
  "name": "@nexora/database",
  "version": "0.1.0",
  "private": true,
  "description": "The only package that talks to Postgres directly (§9.3). Owns api_keys and audit_events — never users or organisations, which live in Clerk (§13.1).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.3",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.11",
    "typescript": "^5"
  }
}
