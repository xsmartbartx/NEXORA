{
  "name": "@nexora/billing",
  "version": "0.1.0",
  "private": true,
  "description": "Stripe integration: plan catalog, webhook verification and subscription sync. Console/Entitlements read through this — never Stripe's API directly.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nexora/database": "*",
    "stripe": "^22.6.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
