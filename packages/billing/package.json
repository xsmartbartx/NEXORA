{
  "name": "@nexora/billing",
  "version": "0.1.0",
  "private": true,
  "description": "Paddle integration: plan catalog, webhook verification and subscription sync. Console/Entitlements read through this — never Paddle's API directly.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nexora/database": "*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
